@model OrderResponse
@{
    ViewBag.Title = "Guard - Order details";
    ViewBag.CurrentUrl = "~/Guard/ViewOrderDetails";
}

<div class="flex">
    <div class="flex-1">
        <div class="text-grey">
            <span>Guard Page</span>
            <i class="fa-solid fa-caret-right"></i>
            <span class="text-grey">Order details</span>
        </div>
    </div>
</div>



<div class="form-container">
    <a asp-controller="Guard" asp-action="Index" class="link-hover">Back to Territories List</a>
    <h2 class="text-large">Order details</h2>
    <h3>Are you sure to delete this order?</h3>
    <form asp-controller="Guard" asp-action="ViewOrderDetails" method="post">
        <input type="hidden" asp-for="Id"></input>
        @*TypeOfService*@
        <div class="form-field flex">
            <div class="w-25">
                <label asp-for="TypeOfService" class="form-label pt">Type of service</label>
            </div>
            <div class="flex-1">
                <input asp-for="TypeOfService" class="form-input" disabled="disabled" />
                <span asp-validation-for="TypeOfService" class="text-red"></span>
            </div>
        </div>
        @*SecurityLevel*@
        <div class="form-field flex">
            <div class="w-25">
                <label asp-for="SecurityLevel" class="form-label pt">Security level</label>
            </div>
            <div class="flex-1">
                <input asp-for="SecurityLevel" class="form-input" disabled="disabled" />
                <span asp-validation-for="SecurityLevel" class="text-red"></span>
            </div>
        </div>
        @*Submit*@
        <div class="form-field flex">
            <div class="w-25">
            </div>
            <div class="flex-1">
                <button class="button button-red-back">Unpin from order</button>
                <div asp-validation-summary="All" class="text-red"></div>
                @if (ViewBag.Errors != null)
                {
                    <div class="text-red ml">
                        <ul>
                            @foreach (string error in ViewBag.Errors)
                            {
                                <li class="ml">@error</li>
                            }
                        </ul>
                    </div>
                }
            </div>
        </div>
    </form>
    <div class="form-field flex">
        <div class="w-25">
            <label class="form-label pt">Owner email</label>
        </div>
        <div class="flex-1">
            <input class="form-input" value="@ViewBag.OwnerEmail" disabled="disabled" />
        </div>
    </div>
    <div class="form-field flex">
        <div class="w-25">
            <label class="form-label pt">Oriented pay per hour value</label>
        </div>
        <div class="flex-1">
            <input class="form-input" value="@ViewBag.OrientedPayPerHour" disabled="disabled" />
        </div>
    </div>
</div>

@section scripts
{
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js" asp-fallback-test="window.jQuery" asp-fallback-src="~/jquery.min.js"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.20.0/jquery.validate.min.js" asp-fallback-test="window.jQuery.validator" asp-fallback-src="~/jquery.validate.min.js"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validation-unobtrusive/4.0.0/jquery.validate.unobtrusive.min.js" asp-fallback-test="window.jQuery.validator.unobstrusive" asp-fallback-src="~/jquery.validate.unobtrusive.min.js"></script>
}